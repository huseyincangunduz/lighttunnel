# TCP_LISTEN ortamı — Proxy sunucusu
TCP_LISTEN_HOST=0.0.0.0
TCP_LISTEN_PORT=14400

# TLS sertifikaları (proxy sunucu sertifikası)
TLS_CA=./infrastructure/certs/ca.crt
TLS_CERT=./infrastructure/certs/server.crt
TLS_KEY=./infrastructure/certs/server.key

# HTTP proxy portu
PROXY_PORT=12180
